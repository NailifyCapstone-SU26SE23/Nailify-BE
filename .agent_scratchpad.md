## Current Goal
Verified logic for Kịch Bản 5 (Emergency Artist Absence) and added automatic promotion voucher assignment for RescheduleSuggested and Cancelled events.

## Progress / Completed Steps
- [x] Injected `IPromotionService` into `NailArtistEmergencyService.cs`.
- [x] Added `_promotionService.AddVoucherForRescheduleAsync(x.BookingId)` when emergency off proposes a rescheduled slot (+/- 30-60 mins).
- [x] Added `_promotionService.AddVoucherForCancelledAsync(x.BookingId)` when emergency off cancels a booking due to no available replacement artist/slot.
- [x] Verified `POST /api/payments/fullRefund/{bookingId}` endpoint in `PaymentsController.cs`.

## Key Decisions Made
- Both Reschedule and Cancel cases in Emergency Off now automatically grant the appropriate compensation voucher to customer's wallet.

## Remaining TODOs
- None. Inform user clearly.
