# Agent Scratchpad

## Current Goal
Enhance `WalkInQueueService.cs` to correctly extract selected items from `SelectedItemsJson` for queue wait-time simulation and artist assignment while preserving Lazy Creation of `BookingProcedure` entities.

## Progress / Completed Steps
- [x] Analyzed issue where `SelectedItemsJson` was created in `AddToQueueAsync` and used in `ConvertWalkInToBookingAsync`, but ignored in `AssignArtistAsync` and `CalculateEstimatedWaitTimeAsync`.
- [x] Formulated architectural decision (Lazy Creation approved by user).
- [x] Created `implementation_plan.md`.

## Key Decisions Made
- Keep Lazy Creation pattern: do not create `Booking` or `BookingProcedure` DB entities during `AddToQueueAsync`.
- Extract `SelectedItemsJson` using a unified helper method in `WalkInQueueService.cs` for simulation in `AssignArtistAsync` and `CalculateEstimatedWaitTimeAsync`.

## Remaining TODOs
- [ ] Await user approval of implementation plan.
- [ ] Add helper method `ExtractQueueItems` to `WalkInQueueService.cs`.
- [ ] Refactor `AssignArtistAsync` to use `ExtractQueueItems`.
- [ ] Refactor `CalculateEstimatedWaitTimeAsync` to use `ExtractQueueItems`.
- [ ] Run `dotnet build` to verify compilation.
